using UnityEngine;
using System.Collections;

public class HulkGridManager : MonoBehaviour {
	public int gridWidth = 1;
	public int gridHeight = 1;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	
	/**
	 * Takes an input point and find out what grid the player should be moving to and returns the center of that grid
	 **/
	Vector3 MoveToGridCoord(Vector3 dest) {
		return new Vector3();	
	}
}
